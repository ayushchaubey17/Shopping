import React, { useReducer } from 'react'

let initialState1=  0;
let reducer2  = (state,action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }

}

export default function UseReducer3() {

let [count,dispatch] = useReducer(reducer2,initialState1)

  return (
    <div>
      
    </div>
  )
}
